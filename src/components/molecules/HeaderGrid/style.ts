import { styled } from '@mui/system'

export const Wrapper = styled('div')({
  height: '100%',
  display: 'flex',
  flexDirection: 'row',
  alignItems: 'center',
  justifyContent: 'space-between',
})
